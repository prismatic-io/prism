query GetIntegrationFlows($id: ID!, $after: String, $first: Int) {
  integration(id: $id) {
    flows(after: $after, first: $first) {
      nodes {
        id
        stableKey
        name
        description
        testUrl
        trigger {
          action {
            component {
              key
            }
            isPollingTrigger
            scheduleSupport
          }
        }
      }
      pageInfo {
        hasNextPage
        endCursor
      }
    }
  }
}
