mutation UpdateIntegrationFlowListeningMode($integrationId: ID!, $isListening: Boolean) {
  updateWorkflowTestConfiguration(input: { id: $integrationId, listeningMode: $isListening }) {
    errors {
      field
      messages
    }
  }
}
