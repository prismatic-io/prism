query GetExecutionSections($executionId: ID!, $sectionIds: [UUID!]) {
  executionSections(
    executionResult: $executionId
    sectionId_In: $sectionIds
    orderBy: { field: STARTED_AT, direction: ASC }
  ) {
    nodes {
      sectionId
      label
    }
  }
}
