import { componentManifests } from "@prismatic-io/spectral";
<%_ components.public.forEach((component) => { _%>
import <%= utils.camelCase(component) %> from "@component-manifests/<%= component %>";
<%_ }); _%>
<%_ components.private.forEach((component) => { _%>
import <%= utils.camelCase(component) %> from "<%= customPrefix %>/<%= component %>";
<%_ }); _%>

export const componentRegistry = componentManifests({
<%_ components.public.forEach((component) => { _%>
  <%= utils.camelCase(component) %>,
<%_ }); _%>
<%_ components.private.forEach((component) => { _%>
  <%= utils.camelCase(component) %>,
<%_ }); _%>
});
