```bash
# Create an instance given a specific $INTEGRATION_ID, customer
#  $CUSTOMER_ID and a required config variable ("My Endpoint"):
prism instances:create \
    --name 'Pull and process SFTP data' \
    --description 'Pull and process SFTP data for Thompson Family Farms' \
    --integration ${INTEGRATION_ID} \
    --customer ${CUSTOMER_ID} \
    --config-vars "[
        {
            \"key\": \"My Endpoint\",
            \"value\": \"https://example.com/api\"
        },
    ]"
```
