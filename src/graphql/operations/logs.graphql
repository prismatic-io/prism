query logs($executionId: ID!) {
  executionResult(id: $executionId) {
    logs(orderBy: { field: TIMESTAMP, direction: ASC }) {
      nodes {
        timestamp
        severity
        message
      }
    }
  }
}
